version: '3.5'

services:
  app:
    build:
      context: ./
      dockerfile: Dockerfile
    image: backend
    volumes:
      - ./:/usr/src/app
      - /cloudsql:/cloudsql
    ports:
      - 5000:5000
    container_name: backend